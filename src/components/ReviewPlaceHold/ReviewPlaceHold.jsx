import css from "./ReviewPlaceHold.module.css";

function ReviewPlaceHold() {
  return (
    <h2 className={css.placetext}>
      Looks like no one has shared their thoughts yet. Be the first to write a
      review!
    </h2>
  );
}

export default ReviewPlaceHold;
